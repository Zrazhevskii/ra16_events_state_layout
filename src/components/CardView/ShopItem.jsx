import React from 'react'

export const ShopItem = ({ item }) => {
    // console.log(item);
    const product = item;
    console.log(product)
    return (
        <div>

        </div>
    )
};

export default ShopItem;
